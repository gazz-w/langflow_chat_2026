viajandocomananda.com, www.viajandocomananda.com {
    reverse_proxy flask:5000
}

# Acesso à UI do Langflow (gestão do fluxo) — opcional.
# Para habilitar: descomente, gere uma senha com:
#   docker run --rm caddy:alpine caddy hash-password
# e crie um A record para langflow.viajandocomananda.com → 91.99.24.48
#
# langflow.viajandocomananda.com {
#     basicauth {
#         admin HASH_GERADO_AQUI
#     }
#     reverse_proxy langflow:7860
# }
